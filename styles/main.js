import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  centered: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  userView: {
    borderWidth: 1,
    borderColor: 'black',
    padding: 5,
    margin: 5,
  },
});
